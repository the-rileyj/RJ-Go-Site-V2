<!DOCTYPE html>
<html>
    {{template "defaulthead"}}
    <body>
        {{template "header"}}
        <div class="container-fluid">
            <div class="col-sm-2">
            </div>
            <div class="col-sm-6">
                <div class="container-fluid no-gutters " id="inner">
                    <div class="row no-gutters">
                        <div class="col-sm-8">
                            <input placeholder="Username" id="username" type="text">
                        </div>
                        <div class="col-sm-4 extra-col">
                            ~Username
                        </div>
                    </div>
                    <div class="row no-gutters">
                        <div class="col-sm-8">
                            <input placeholder="Password" id="password" type="password">
                        </div>
                        <div class="col-sm-4 extra-col">
                            ~Password
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-sm-2 contigous-bg" id="send">
                Send
            </div>
            <div class="col-sm-2">
            </div>
        </div>
        {{template "footer"}}
        <!--div.super-spacer-->
        <!--script type="text/javascript">
            function getWS() {
                var socket = new WebSocket("ws://therileyjohnson.com/wsspying");
                return socket;
            }
        </script-->
    </body>
</html>